{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  
  // Connection string configuration
  // For local development: Uses SQLite (see below)
  // For production (Render): Set DATABASE_URL environment variable (PostgreSQL)
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=coffee_loyalty.db"
  }
}